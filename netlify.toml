[build]
  command = "chmod +x node_modules/.bin/vite 2>/dev/null || true && npm run build"
  publish = "dist"
  ignore = "git diff --quiet $CACHED_COMMIT_REF $COMMIT_REF -- . ':!.netlify'"

[build.environment]
  NODE_VERSION = "22"
  NPM_FLAGS = "--include=dev"
